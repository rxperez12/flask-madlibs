{% extends 'base.jinja' %}

{% block title %} Madlibs! {% endblock %}

{% block content %}

  <h1 class="pt-4 mb-3 text-center">Madlibs!</h1>

  <div class="ms-3 me-3">
    <form action="/results" class="pb-4">

    <!-- an example of one field -- you should make these autogenerated! -->
      <div class="mb-3 row">
        {% for word in word_prompt%}     <!--FIXME: word_prompts plural, fix aria-label -->
        <label class="col-5 col-form-label text-end">{{ word }}</label>
        <div class="col-7">
            <input
            name="{{word}}"
            required
            aria-label="noun"
            class="form-control">

        </div>
      {% endfor %}

      </div>

      <button class="btn btn-primary btn-lg w-100">
        Make My Story!
      </button>
    </form>
  </div>

{% endblock %}
